# Velpro Blog 项目通览与 AI 面试梳理

## 口径先说明

这个仓库本体，严格来说是一个 **个人博客 + 知识库 + 项目展示 + 后台内容管理系统**，核心技术栈是 **Vue 3 + Vite + TypeScript + Pinia + Element Plus + Markdown-it**。  
它本身并没有直接接入大模型 API，也不是一个纯 AI 产品。

但是，这个项目里有两个和面试强相关的点：

1. 它本身具备比较完整的内容平台能力，能体现前端工程化、页面设计、内容管理、搜索检索、Markdown 渲染、状态管理、权限控制这些能力。
2. 在“项目展示”和“个人介绍”页面里，明确承载了一个 AI 项目案例：**CVita - AI 心理健康助手**。所以如果面试官重点问 AI，可以基于这个项目案例继续展开。

建议你的面试表述口径是：

“我做的这个项目，主体是一个前端知识博客和内容管理平台，它解决的是内容展示、知识沉淀和后台维护的问题。同时我在项目展示模块里也沉淀了自己做过的 AI 项目案例，比如 CVita 这个 AI 心理健康助手，所以如果面试官对 AI 感兴趣，我可以从实际项目落地角度继续展开。”

---

## 一、项目描述

### 1. 项目定位

Velpro Blog 是一个面向前端开发者的个人技术博客系统。它不只是一个展示型首页，而是把 **内容阅读、知识检索、项目展示、个人介绍、后台维护** 串成了一个完整闭环。

前台部分负责“看”和“找”：

- 首页负责建立第一印象，突出项目调性和技术能力。
- 知识库页面负责沉淀技术文档，支持搜索、阅读和回顾。
- 项目展示页负责把项目经验结构化呈现出来。
- 个人介绍页负责把个人经历、技能和项目经验整合成在线简历。

后台部分负责“写”和“管”：

- 通过登录进入内容管理后台。
- 可以对知识库文档进行新增、编辑、删除和预览。
- 支持本地草稿保存，适合长期维护内容。

### 2. 项目目标

这个项目想解决的不是“做一个简单博客页”，而是解决以下几个更实际的问题：

- 技术内容如何长期沉淀，而不是散落在本地笔记里。
- 内容如何既适合阅读，又适合检索和回看。
- 项目经验如何不是一堆文字，而是以页面化、结构化的方式展示给面试官或招聘方。
- 前台展示和后台维护如何形成一个统一系统，而不是两个割裂页面。

### 3. 技术特点

这个项目的技术特点比较明确：

- 用 Vue 3 + Composition API 组织页面逻辑，结构清晰，便于拆分。
- 用 Vite 做构建，首屏开发体验快，页面按路由懒加载。
- 用 Pinia 做状态管理，并通过持久化插件把用户态、文档态保存在本地。
- 用 Markdown-it + Highlight.js 处理知识文档渲染和代码高亮。
- 用 Fuse.js 做模糊搜索，提升知识库和项目页的检索体验。
- 用 GSAP 和 Vanta 做首页氛围动效，让页面有更强的视觉记忆点。
- 用 Element Plus 承担后台表单、表格、弹窗等中后台交互。

一句话概括，这个项目是一个 **偏内容平台属性、兼顾展示效果和后台管理能力的前端作品**。

---

## 二、实现功能有哪些

这一部分面试时不用讲代码细节，重点讲“这个系统能做什么”。

### 1. 前台展示功能

- 支持首页内容展示，包括主视觉、技术标签、能力指标、内容闭环介绍。
- 支持知识库内容浏览，文档可按列表切换阅读。
- 支持知识库模糊搜索，按标题和标签快速筛选文章。
- 支持 Markdown 文档渲染，适合展示技术型内容。
- 支持代码高亮，提升技术文章可读性。
- 支持阅读进度条，增强长文阅读反馈。
- 支持项目展示，按分类和关键词筛选项目。
- 支持项目详情抽屉展开，展示职责、能力点和项目结果。
- 支持个人介绍页，展示个人信息、技能、教育背景、成长路径和项目经历。
- 支持亮色 / 暗色主题切换。
- 支持移动端和桌面端响应式适配。

### 2. 后台管理功能

- 支持登录认证。
- 支持路由鉴权，未登录不能直接进入后台页面。
- 支持知识库文档列表查看。
- 支持新增文档。
- 支持编辑已有文档。
- 支持删除文档。
- 支持文档标签管理。
- 支持文档摘要维护。
- 支持 Markdown 内容实时预览。
- 支持编辑、预览、分栏三种编辑模式切换。
- 支持快捷插入标题、代码块、引用、列表等常用 Markdown 片段。
- 支持图片上传并插入到 Markdown 内容中。
- 支持本地草稿自动保存。
- 支持未保存内容关闭提醒。
- 支持文档字数、标题数、预计阅读时长统计。

### 3. 工程和交互层面的功能

- 支持页面切换动效。
- 支持首页分阶段加载，降低首屏压力。
- 支持部分模块异步加载。
- 支持状态持久化，本地刷新后仍能保留部分数据。
- 支持登录后自动跳转到原目标页面。
- 支持统一布局和统一导航。

如果要用一句比较像项目介绍的话来说，可以这样讲：

“这个系统不只是一个静态博客，而是做成了前台内容展示、知识检索、项目介绍和后台文档维护一体化的平台，既能展示，也能管理。”

---

## 三、页面组件和页面观感

这一部分的目标，是让面试官脑子里能出现画面。

### 1. 首页

首页不是传统那种简单 Banner，而是比较有产品感的技术展示首页。

页面顶部是一个固定的玻璃态导航栏，左边是站点标识，右边是首页、知识库、项目展示、个人介绍这些主路由，同时带有 GitHub 入口、主题切换按钮和后台入口按钮。

首页首屏的主视觉非常明显：

- 左侧是一组大标题，核心文字是“阅读 · 创作 · 管理”。
- 下面会配一段对平台定位的描述，强调它是一个面向前端开发者的知识博客系统。
- 标题下方还有一组技术标签，比如 Vue 3、TypeScript、GSAP、Markdown、Element Plus、Vanta HALO。
- 再往下是两个入口按钮，一个进知识库，一个看项目页。
- 底部还有三个指标卡片，用“核心模块”“动效场景”“内容闭环”这种方式强化项目印象。

首页右侧不是普通配图，而是一个模拟“产品面板”的内容区：

- 顶部有类似窗口控制点的装饰。
- 中间是 Vanta HALO 的动态背景效果。
- 下方再用几张信息卡强调“动画层”“内容引擎”“使用体验”。

整体观感是：**偏技术作品集风格，既有展示感，也有一点沉浸式氛围。**

### 2. 首页下半部分

首页不是只有一屏，它下面还有两个承接区块：

- 一个是“核心价值”区块，用三张卡片讲这个系统的价值，比如快、好找、能沉淀。
- 一个是“内容流转”区块，用 Step 1、Step 2、Step 3 的方式讲清楚首页引流、知识库沉淀、后台维护三者如何形成闭环。

这个设计很好讲，因为它不只是页面堆内容，而是在表达产品路径：

“先吸引你进来，再让你找到内容，最后通过后台把内容持续维护起来。”

### 3. 知识库页面

知识库页面是这个项目最像“产品”的页面。

最上面先是一个 Hero 区：

- 左边写“知识库”定位和一句比较大的标题。
- 右边是一个最近同步时间卡片，下面会显示当前正在看的文章标题。

主体是左右两栏布局：

- 左边是文档列表区，包含搜索框、文档数量、文章卡片列表。
- 每一篇文章卡片会显示标题、日期、摘要和标签。
- 当前选中的文章会高亮，视觉上很明确。

- 右边是文章阅读区。
- 文章顶部会先展示创建时间、标签数量、标签列表、标题和摘要。
- 再下面是 Markdown 渲染出来的正文内容。
- 如果文档还在渲染，会先显示骨架屏。
- 页面顶部还有一个阅读进度条，滚动时能实时看到阅读进展。

这个页面给人的感觉是：**像一个轻量级技术知识库产品，而不是简单的文章详情页。**

### 4. 项目展示页

项目展示页更像作品集页。

页面顶部有一个统一的 SectionTitle，先告诉面试官这里是项目集合。  
下面是一块搜索和筛选区域：

- 左边是搜索输入框，可以搜项目名称、技术栈和亮点。
- 右边是一组分类按钮，比如 AI 服务平台、企业服务平台、微信小程序。

项目主体是一个卡片网格：

- 每张卡都会展示项目类型、项目标题、项目周期、项目简介。
- 卡片中间会列出技术栈标签。
- 卡片下方会列出项目亮点。
- 最后可以点“查看项目详情”。

点开之后不是跳转新页，而是右侧抽屉展开详情：

- 先看项目概要和技术栈。
- 再看职责与角色。
- 再看核心能力点。
- 最后看项目结果。

这个页面最大的优点，是把项目经验从“简历上的一段字”变成了“可以筛选、可以浏览、可以展开”的结构化内容。

### 5. 个人介绍页

个人介绍页本质上是一个在线简历页面。

它采用左右分栏布局：

- 左边是一个相对固定的侧边信息栏，包含姓名、岗位方向、联系方式、专业技能。
- 每个技能组都做成独立卡片，比如基础、框架、工程化。

- 右边是主内容区，依次展示项目经验、教育背景、技术成长路径。
- 项目经验部分采用卡片式排布，每个项目展示技术栈、项目摘要和个人职责。
- 教育背景是简洁卡片。
- 成长路径是时间线形式，适合讲自己的成长节奏。

这个页面的感觉是：**既像简历，又比简历更有页面化表达。**

### 6. 登录页

登录页是一个偏中后台入口风格的页面。

- 左侧在大屏下会展示一个介绍区，说明这是知识库后台管理系统。
- 右侧是玻璃态登录面板，包含欢迎语、账号密码输入框和登录按钮。
- 背景有带渐变感的光斑装饰，不会显得很死板。

所以它不是单纯一个表单页，而是有一点产品后台入口的氛围感。

### 7. 后台管理页

后台采用典型的工作台布局。

- 左侧是竖向导航区，展示工作台标题、当前用户信息和后台菜单。
- 右侧顶部是工作区头部，展示当前模块标题、最近同步时间和主题切换。
- 下方是正文内容区。

编辑器页面是整个后台最核心的部分：

- 顶部先是模块标题和新增、刷新按钮。
- 下面有一个提示条，明确当前是静态演示模式。
- 再往下是三个概览卡片，展示文档数量、当前文档、最近同步时间。
- 中间是一张文档表格，直接看标题、简介、标签、创建时间和操作按钮。

点击新增或编辑后，会打开一个超大的弹窗编辑器：

- 上方先用四个卡片展示字数、标题数、预计阅读、草稿状态。
- 中间有一个工具栏，可以切换“仅编辑 / 分栏 / 仅预览”。
- 左边是表单和 Markdown 输入区。
- 右边是实时预览区。
- 底部再放保存和取消按钮。

这个后台页面的优势，是让面试官能明显感觉到：**这不是一个展示型项目，而是具备真实维护体验的内容系统。**

---

## 四、重点：AI 应用

这一部分是面试时最容易说偏的，所以建议你分成两层来讲。

### 1. 第一层：如实说明当前仓库的 AI 边界

如果严格只看这个仓库代码，它没有直接接入 OpenAI、DeepSeek、Claude 这类模型接口，也没有完整的对话窗口、Prompt 管理、Embedding、RAG 检索这些典型 AI 工程代码。

所以最稳的说法是：

“当前这个仓库本体不是 AI 产品，它主要是一个知识博客和内容管理平台；但是我在这个项目里把自己做过的 AI 项目经验结构化沉淀进了项目展示模块，其中最典型的是 CVita - AI 心理健康助手。面试如果需要，我会从这个 AI 项目案例继续展开。”

这句话很重要，因为它能避免面试时被追问到当前仓库里根本不存在的模型调用细节。

### 2. 第二层：当前仓库里可以讲的“智能化思路”

虽然这里没有直接接大模型，但它已经具备一些“智能内容平台”的基础能力，可以作为 AI 产品前台的底座去讲：

- 通过 Fuse.js 实现知识库和项目页的模糊搜索，让内容检索更高效。
- 通过 Markdown 渲染和代码高亮，把知识内容结构化、可阅读化。
- 通过后台 CMS 管理文档，让知识能够持续沉淀，而不是散落。
- 通过标签、摘要、列表、详情这些信息结构，为后续 AI 检索或知识召回留出内容基础。

如果面试官问“这个项目和 AI 有什么关系”，你可以这么回答：

“它本身不是模型产品，但它做的是 AI 内容系统里非常重要的一层，也就是内容承载层和知识管理层。很多 AI 应用最后都离不开知识沉淀、内容管理、结构化展示和后台运营，这个项目其实把这些底座能力先做出来了。”

### 3. 第三层：真正的 AI 项目主线建议讲 CVita

根据项目展示页和个人介绍页里的信息，CVita 是一个 **AI 心理健康助手**，项目包含以下关键模块：

- 智能对话
- 情绪日记
- 风险分级
- 内容知识库
- 后台 CMS
- 运营分析看板

你的角色可以概括成：

“我负责前端架构设计和核心模块开发，覆盖用户端和后台管理端，重点包括 SSE 流式对话、情绪识别结果展示、知识库 CMS、数据可视化和通用工程能力沉淀。”

### 4. AI 应用的整体实现思路

这一段建议你按“前台交互层 - AI 服务层 - 内容层 - 后台运营层”去讲，逻辑最清楚。

#### 4.1 前台交互层

用户端至少包含三类核心页面或模块：

- AI 对话页面：用户发起问题，系统流式返回回答。
- 情绪日记页面：用户记录情绪内容，系统做分类、打标签或风险提示。
- 内容推荐 / 知识辅助区：把适合的科普内容、心理建议或知识文章展示给用户。

前端要做的不是“自己算 AI”，而是把 **输入、响应、状态、交互反馈** 组织好。

#### 4.2 AI 服务接入层

这一层建议你在面试里这样表述：

“前端不会直接把模型能力硬编码在页面里，而是通过后端统一封装模型服务，前端以接口或 SSE 的方式消费结果。这样做的好处是模型切换更灵活，鉴权、安全、日志和风控也更集中。”

如果面试官问为什么用 SSE，可以回答：

- 对话场景本质是服务端单向持续返回文本，SSE 很适合这种流式输出。
- 相比 WebSocket，SSE 在实现和接入层面更轻，尤其适合以 HTTP 体系为主的后台服务。
- 前端更容易处理流式文本拼接、完成标记和异常中断重试。

#### 4.3 业务能力组织

CVita 的 AI 能力不是只做一个聊天框，而是围绕心理健康场景做业务闭环：

- 对话模块负责实时交互。
- 情绪日记负责长期行为记录。
- 风险分级负责识别潜在高风险用户状态。
- 知识库负责承载标准化内容。
- CMS 负责维护知识内容和运营素材。
- 数据看板负责观察业务使用情况。

也就是说，它不是“单点 AI”，而是“AI + 内容 + 运营 + 风控”的组合型产品。

#### 4.4 知识库和 AI 的结合方式

这个点很适合面试官追问，所以建议主动讲：

“AI 的回答不能只靠模型自由发挥，尤其在心理健康这种敏感场景，必须结合可控内容。我的思路是把知识库内容先管理起来，由后台去维护标准化内容、标签和分类；前端一方面承接对话结果，另一方面把内容推荐、知识浏览和后台管理串起来，形成更可控的应用结构。”

这个回答的优点是：

- 体现你知道 AI 产品不能只靠模型。
- 体现你理解内容可信度和运营可维护性的重要性。
- 也能把当前博客项目和 AI 项目自然连接起来。

### 5. 面试高频问题怎么结合项目回答

这一段你可以直接背思路。

#### 问题 1：你做的 AI 项目里，前端主要负责什么？

建议回答：

“前端主要负责三件事。第一是把用户输入、对话流、状态反馈和内容展示做好，保证 AI 交互体验完整；第二是把情绪日记、知识库、后台 CMS、数据看板这些业务模块串起来，形成闭环；第三是做工程层的抽象，比如请求封装、状态管理、路由权限、通用组件复用，保证项目可以持续迭代。”

#### 问题 2：为什么要用 SSE？

建议回答：

“因为这个场景主要是服务端持续往前端推送生成中的文本结果，本质上是单向流式输出。SSE 基于 HTTP，落地更轻，和现有接口体系兼容也更好。前端拿到数据后可以一边接收一边渲染，用户体感会比等整段返回更自然。”

#### 问题 3：AI 对话的前端难点是什么？

建议回答：

“难点主要在状态管理和异常处理。比如用户消息、AI 消息、流式中间态、结束态、失败态都要区分；另外还要处理取消生成、重复提交、滚动定位、网络中断、刷新恢复这些体验问题。真正复杂的不是把文本显示出来，而是把整个对话过程管理稳定。”

#### 问题 4：AI 项目怎么和知识库结合？

建议回答：

“我的理解是，AI 只负责生成和理解能力，知识库负责提供可控内容。知识库经过结构化维护后，可以支撑内容推荐、标准答案、运营维护甚至后续检索增强。前端层面我要做的是把知识内容展示、搜索、后台维护这些基础能力先搭起来，让 AI 的结果不是孤立输出。”

#### 问题 5：情绪识别和风险分级的价值是什么？

建议回答：

“它让项目不只是一个聊天工具，而是真正进入业务场景。用户在对话和日记中表达的内容，可以被进一步转成情绪标签、趋势记录和风险等级，然后反馈到提醒机制、内容推荐和后台监控中。这种结构能让 AI 项目更像产品，而不是 Demo。”

#### 问题 6：你在这个 AI 项目里最能体现价值的地方是什么？

建议回答：

“我觉得不是单个页面，而是我把 AI 对话、内容知识库、后台 CMS 和数据看板这些模块连接成了完整的产品结构。这样项目既有前台用户体验，也有后台运营支撑，后续无论是模型升级还是内容扩展，都更容易持续迭代。”

### 6. AI 项目里可能遇到的问题，以及怎么解决

这一段非常适合面试，因为能体现你不是只会讲流程，而是真的理解落地问题。

#### 问题 1：SSE 流式返回过程中容易中断或重复

可能现象：

- 网络不稳定时回答中断。
- 重新连接后文本重复拼接。
- 用户连续发送消息导致上下文混乱。

解决思路：

- 为每次会话和每条消息建立唯一标识。
- 前端按消息状态维护“生成中、完成、失败、取消”。
- 流式拼接时按当前消息单独缓存，结束后再写入正式历史。
- 对重复片段或重复结束事件做幂等处理。

#### 问题 2：长对话场景下页面状态复杂

可能现象：

- 页面滚动总被打断。
- 对话太长导致渲染压力增大。
- 用户切换会话时容易丢上下文。

解决思路：

- 把“会话列表”和“消息列表”分层管理。
- 当前会话、历史会话、临时生成态分开存。
- 只对当前活跃内容做实时更新，避免全量重渲染。
- 对滚动行为做控制，用户手动上滑时不要强制拉到底部。

#### 问题 3：AI 输出不可完全控

可能现象：

- 心理健康场景里，模型回答可能过泛、过空或者不够谨慎。
- 高风险内容如果处理不好，会带来业务风险。

解决思路：

- 不把模型输出当最终真相，而是结合知识库内容和业务规则。
- 对高风险场景做额外分级和提示策略。
- 通过后台可维护内容，把稳定建议沉淀成标准化内容。
- 前端在交互上明确提示回答仅供参考，并保留人工介入空间。

#### 问题 4：知识内容更新和 AI 能力容易脱节

可能现象：

- 前台推荐内容过时。
- 后台维护内容和用户实际问题不匹配。
- 项目后期难迭代。

解决思路：

- 把知识内容做成 CMS 管理，而不是写死在页面。
- 用标签、摘要、分类这些结构化字段管理内容。
- 通过数据看板观察用户使用路径和热点问题，再反向优化内容。

### 7. 面试时最稳的一段总结话术

你可以用下面这段作为 AI 部分的收口：

“如果严格看这个仓库，它本身更偏内容平台和知识管理系统，没有直接写模型接入代码；但它已经把知识沉淀、内容检索、后台管理这些 AI 产品非常依赖的底层能力搭出来了。真正的 AI 项目经验，我在 CVita 这个 AI 心理健康助手里做了比较完整的落地，重点覆盖了 SSE 流式对话、情绪识别结果承接、知识库 CMS、数据看板和前后台协同，所以我对 AI 项目的理解不是停留在聊天框，而是更偏向产品化落地。”

---

## 最后给你的建议

如果面试官问这个项目，你不要一上来就说“这是个博客”。  
更好的说法是：

“这是我做的一个前端内容平台项目，前台负责知识展示、项目展示和个人能力表达，后台负责内容管理和持续沉淀。它本身也承担了我的项目作品集和知识库角色。AI 方面，这个仓库主要做内容底座，而我在其中沉淀了一个 AI 心理健康助手项目案例，可以继续展开讲业务闭环和 AI 落地思路。”

这样说，层次会明显更高。
